node_modules/
dist/
dev-dist/
*.local
.DS_Store
repro-tmp.ts
test-results/
playwright-report/

# throwaway measurement output from investigation harnesses
*-out.txt

# Simulation output: regenerated by `npm run sim`, not source.
sim-*.json

# Content packages currently being authored. Named individually rather than by
# a *Depth.ts glob: five long-standing files match that pattern, and ignoring
# them would silently drop real content the moment anyone re-added them.
# Delete these lines as each package lands.

# Authoring scratch. Agents write these while working and delete them after;
# committed once by accident, so they are ignored rather than remembered.
*.selfcheck.test.ts
*.scan.test.ts
selfcheck-*.txt
